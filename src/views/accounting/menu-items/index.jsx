import AccountingPages from './AccountingPages';
import Account from './Account';
import otherSubmenuItems from './Others';

const menuItems = {
  items: [AccountingPages, Account, otherSubmenuItems]
};

export default menuItems;
